#ifndef HELPERS_H
#define HELPERS_H

#include <algorithm>
#include <string>

class Helpers
{
public:
    static std::string toLower(const std::string &str);
    static std::string toUpper(const std::string &str);
};

#endif